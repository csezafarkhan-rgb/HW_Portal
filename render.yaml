services:
  - type: web
    name: hw-portals
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /healthz
    autoDeploy: true
    envVars:
      - key: APP_USER
        sync: false
      - key: APP_PASSWORD
        sync: false
